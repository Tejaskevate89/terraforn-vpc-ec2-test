- name: install apache2
  hosts: all
  become: true
  tasks:
   - name: install httpd if os is readhat
     yum:
      - name: httpd
        state: latest
    - name: install apache2 if os is debian
      apt:
       - name: install apache2
         state: latest